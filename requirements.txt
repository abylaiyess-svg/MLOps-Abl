# MLflow 3.x removed Model Registry stages (transition_model_version_stage),
# on which eval_gate.py depends -> keep the 2.x line.
mlflow>=2.20,<3
scikit-learn>=1.4
pandas>=2.1
numpy<3
boto3>=1.34
psycopg2-binary>=2.9
dvc>=3.50
dvc-s3>=3.2
pytest>=8.0

# `mlflow models serve`: в 2.22.5 сервер скоринга вешает Flask-стиль
# @app.route(...) на объект FastAPI. Декоратор .route живёт в Starlette < 0.47,
# поэтому без этого пина сервинг падает с AttributeError.
fastapi<0.116
